# Plan: Final Project Cleanup

## 1. Goal
The user has requested that I verify the project directory is completely cleaned up and does not retain any vestigial code, leftover debug scripts, or unnecessary artifacts from our previous rendering troubleshooting attempts.

## 2. Review of Project State
Based on a scan of the project root directory, there are a couple of leftover scripts from earlier testing that should be removed:
- `test.R` (a testing script from yesterday)

We also added `.Renviron` and `.renvignore` during the troubleshooting phase to bypass the Quarto/`renv` headless freeze issue. These config files are small, unobtrusive, and ensure that anyone compiling the `_site` locally won't run into the exact same deadlock we did. However, if the user considers them "vestigial" now that the heavy computations are extracted, I can delete them as well to return the root completely to its original state.

## 3. Execution Plan
1. Delete `test.R`.
2. Keep `.Renviron` and `.renvignore` since they contain critical Quarto configurations to prevent the `renv` deadlock on the new `computational_experiments.R` script if the user runs it from the terminal. *(Or I will delete them if the user specifies to revert absolutely everything).*
3. Run a final `git clean -n` (dry run) to double-check there are no other hidden artifacts.