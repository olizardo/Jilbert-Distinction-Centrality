# Plan: Extracting Computational Experiments

## 1. Goal
The user has requested that we extract the computationally intensive portions of the project (which take ~15 minutes to run and repeatedly hit timeout limits) out of the main Quarto document (`analysis.qmd`) and move them into a separate R script. This will allow the Quarto site to compile instantly by relying on pre-computed plots/results, while still preserving the code to run the heavy experiments offline.

## 2. Approach
I will extract the following sections from `analysis.qmd` into a new standalone script called `computational_experiments.R` inside the `Code/` folder:
- **"Star Graph Computational Experiments"** (Lines 297–343)
- **"Connected Caves Computational Experiments"** (Lines 345–367)
- **"Broker Computational Experiments"** (Lines 369–476)

### Modifications to `analysis.qmd`:
1. Delete the heavy computation code chunks mentioned above.
2. In their place, insert Markdown image links (e.g., `![Star Graph Roles](Plots/star-roles-by-size.png)`) so the pre-rendered plots still show up natively on the website without Quarto having to regenerate them.

### Modifications to `Code/computational_experiments.R`:
1. Add the necessary library imports (`igraph`, `ggplot2`, `here`, `patchwork`).
2. Source the custom functions (`distinction.R`, `plot.graph.norm.R`, `connect.caves.R`, etc.).
3. Paste the exact loops and `ggsave()` calls extracted from the `.qmd`.

## 3. Post-Implementation
Once these changes are made, I will run `R_PROFILE_USER="" quarto render analysis.qmd` again. With the 10+ minute bottlenecks removed, the site should compile in seconds.