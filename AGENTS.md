# Project Memory - Distinction Centrality Analysis

This file maintains the current project state, data schemas, code architecture, and key findings to help subsequent assistant sessions resume seamlessly.

## Project Overview
This project implements and analyzes **Distinction Centrality** ($\beta_i$), a social network metric theoretically inspired by Pierre Bourdieu's theory of agonistic competition in social fields. It models the trade-off between status (influence/ eigenvector centrality) and constraint (average status of neighbors in the node-deleted subgraph).

The mathematical formulation is:
$$\beta_i = \alpha s_i - \kappa_i$$
where $\alpha$ is a scalar adjustment factor:
$$\alpha = \frac{\bar{\mathbf{\kappa}}}{\bar{\mathbf{s}}}$$

---

## Data Catalog & Schemas

All data files are located in the `Data/` folder and represent undirected networks as edge lists (two columns representing connected nodes).

| File Name | Format | Rows x Cols | Column 'a' Type | Column 'b' Type | Description |
| :--- | :--- | :--- | :--- | :--- | :--- |
| `Data/MediciEdgeList.csv` | CSV | 42 x 2 | Character | Character | Padgett & Ansell's (1993) Medici family network. |
| `Data/MediciEdgeListMarriageCut.csv` | CSV | 41 x 2 | Character | Character | Medici family network with one critical marriage edge removed. |
| `Data/NoMediciEdgeList.csv` | CSV | 36 x 2 | Character | Character | The Florentine network without the Medici family. |
| `Data/poli1.csv` | CSV | 33 x 2 | Integer | Integer | Political network experiment step 1. |
| `Data/poli2.csv` | CSV | 36 x 2 | Integer | Integer | Political network experiment step 2 (added edges). |
| `Data/poli3.csv` | CSV | 39 x 2 | Integer | Integer | Political network experiment step 3. |
| `Data/poli4.csv` | CSV | 41 x 2 | Integer | Integer | Political network experiment step 4. |
| `Data/searchmodeltest.csv` | CSV | 18 x 2 | Integer | Integer | Network for testing search model dynamics. |

---

## Core Functions Directory

All core custom metrics and modeling scripts are located in the `Functions/` folder:

- **`Functions/distinction.R`**: Calculates distinction centrality ($\beta_i$), scaled distinction, status ($s_i$), constraint ($\kappa_i$), and the scalar factor $\alpha$ for a given `igraph` object.
- **`Functions/tab.cent.R`**: Formats the centrality scores using `kableExtra` and saves them as LaTeX tables inside the `Tabs/` folder.
- **`Functions/plot.graph.norm.R`**: Generates a standard `ggraph` plot of the network. Nodes are continuously colored by their scaled distinction score using a gradient (`dodgerblue` for low/negative distinction $\rightarrow$ `gray90` for neutral $\rightarrow$ `firebrick` for high distinction). Automatically handles label sizing for empirical vs toy networks.
- **`Functions/connect.caves.R`**: Caveman graph generation and random edge additions.
- **`Functions/make.broker.cliques.R`**: Generates brokerage structures over clique subgraphs.
- **`Functions/make.broker.trees.R`**: Generates brokerage structures over tree subgraphs.
- **`Functions/make-bonacich.R`**: Generates networks for Bonacich (2007) comparisons.
- **`Functions/plot.graph.cat.R`**: Categorical visualization of distinction scores.

## Helper Scripts (`Code/`)

- **`Code/diss.status.R`**: Computes status using similarity-discounted adjacency matrix.
- **`Code/make.estrada.R`**: Generates Estrada (2005) graph examples.
- **`Code/plot_constraint_vs_order_circle.R`**: Simulates and plots the decay of constraint in circle graphs as N increases.
- **`Code/plot_small_world_examples.R`**: Generates network visualizations tracing the evolution from a symmetric lattice to a dense random graph.
- **`Code/plot_small_world_evolution.R`**: Simulates the addition of random edges to a regular lattice and plots the resulting variance and mean constraint trajectories.
- **`Code/plot_span_of_control.R`**: Models the shifting leverage between the root and middle managers as the span of control increases.
- **`Code/plot_tree_comparisons.R`**: Generates multi-panel plots comparing distinction across hierarchical trees of varying depths (e.g., Depth 2, 3, 4).

---

## Key Files & Outputs

- **`manuscript.tex`**: The main LaTeX paper source describing the methodology and findings. Recently updated to include analyses of span of control, small-world networks, and improved visualizations for the Medici and Karate Club data.
- **`manuscript.pdf`**: Compiled PDF draft of the manuscript.
- **`Website/`**: Folder containing the Quarto project for the distinction function documentation and web examples. Key pages include `index.qmd`, `functions.qmd`, `examples.qmd`, and the newly added `small-world.qmd`.
- **`Tabs/`**: Folder containing LaTeX table exports generated by the analysis.
- **`Plots/`**: Folder containing generated visual graphs of toy models. All plots utilize a continuous `dodgerblue` $\rightarrow$ `firebrick` color scale.

---

## Quarto Website & Rendering Notes

The project includes a Quarto website (configured in `_quarto.yml`). 
**Important Note on Rendering:** `analysis.qmd` is computationally heavy as it generates dozens of network plots and tables simultaneously. 
- Attempting to add complex HTML/flexbox layouts or using raw `cat()` to format these outputs for the web makes the Quarto render times prohibitively slow. 
- When assisting with the website, keep Quarto outputs standard and minimal. Prefer relying on Quarto's built-in caching (`cache: true` in execute options) if heavy structural changes are needed.

---

## Current Setup & Environment Requirements

To initialize the R environment for any analysis, run:
```r
library(igraph)
library(ggraph)
library(here)
library(dplyr)
library(kableExtra)
library(ggpubr)
library(patchwork)
```

### Installation of LaTeX Compiler (Chromebook/Linux)
TinyTeX is installed in `/home/omarlizardo/.TinyTeX` and its binaries are symlinked to `/home/omarlizardo/bin/`. They can be run via:
```bash
~/bin/pdflatex --version
~/bin/latexmk --version
```
In R, use:
```r
tinytex::is_tinytex() # Should return TRUE
tinytex::latexmk("manuscript.tex") # Compiles and auto-downloads missing TeX packages
```
